Projet de Fin de Cours : Analyse de Données avec SQL
*Formation D-CLIC 3 – Parcours Data*

## 📝 Présentation
Ce projet consiste en la création d'une base de données relationnelle pour la gestion des employés et des départements, suivie d'une série d'analyses statistiques via SQL.

## 📁 Contenu du dépôt
* `ProjetSQL_DOH_Yawo_Marie_Victor.sql` : Script complet contenant la création des tables, les insertions et les requêtes d'analyse.
* `Documentation_ProjetSQL_DOH.pdf` : Documentation détaillée expliquant la démarche et les résultats.

## 🚀 Compétences techniques
* Création de schémas relationnels (`CREATE TABLE`, `PRIMARY KEY`, `FOREIGN KEY`).
* Manipulation de données (`INSERT`, `UPDATE`, `DELETE`).
* Analyses avancées (`JOIN`, `GROUP BY`, `HAVING`, `SUBQUERIES`).
